[tool:pytest]
# Configuration du répertoire des tests
testpaths = tests

# Options par défaut
addopts = -v --tb=short --disable-warnings

# Configuration des marqueurs de tests
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
